apiVersion: v1
kind: Service
metadata:
  name: python-app
  namespace: dev-test
spec:
  selector:
    app: python-app
  ports:
  - port: 5000
    targetPort: 5000

#kubectl port-forward svc/python-app -n dev-test 5000:5000
